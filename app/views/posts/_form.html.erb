
<%= simple_form_for @post do |f| %>
<p>Forum topic</p>
<p><%= f.input :title, label: false%></p>
<p>Message</p>
<%= f.input :content, label: false, input_html: { class: 'form-control' }%>

<%= f.submit "#{@post.new_record? ? 'Create' : 'Edit'}", class: 'btn btn-primary'%>
	<% end %>