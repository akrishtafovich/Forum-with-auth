<%= simple_form_for [@post, @post.comments.build] do |f| %>
    <%= f.input :comment, placeholder: 'Write message',input_html: { class: 'form-control' } %>
    <%= f.submit class: 'btn btn-primary' %> 
<% end %>